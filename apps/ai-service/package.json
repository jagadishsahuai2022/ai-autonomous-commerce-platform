{
  "name": "ai-service",
  "version": "1.0.0",
  "description": "Python FastAPI AI service for recommendations",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "start": "python -m uvicorn app.main:app --port 8000"
  }
}
